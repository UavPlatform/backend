services:
  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-drone_db}
      MYSQL_USER: ${MYSQL_USER:-drone}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-change_me}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-change_root_password}
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    ports:
      - "${MYSQL_PORT:-3306}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot -p$${MYSQL_ROOT_PASSWORD} --silent"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      TZ: ${TZ:-Asia/Shanghai}
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/${MYSQL_DATABASE:-drone_db}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
      SPRING_DATASOURCE_USERNAME: ${MYSQL_USER:-drone}
      SPRING_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-change_me}
      DRONE_DATASOURCE_DRIVER_CLASS_NAME: com.mysql.cj.jdbc.Driver
      DRONE_DATASOURCE_HOST: mysql
      DRONE_DATASOURCE_PORT: "3306"
      DRONE_DATASOURCE_DATABASE: ${MYSQL_DATABASE:-drone_db}
      DRONE_DATASOURCE_USERNAME: ${MYSQL_USER:-drone}
      DRONE_DATASOURCE_PASSWORD: ${MYSQL_PASSWORD:-change_me}
      DRONE_JWT_SECRET: ${JWT_SECRET:-replace-with-a-strong-secret-at-least-32-chars}
      DRONE_JWT_EXPIRATION: ${JWT_EXPIRATION:-3600000}
      DRONE_JWT_REFRESH_EXPIRATION: ${JWT_REFRESH_EXPIRATION:-604800000}
      DRONE_TRTC_SDK_APP_ID: ${TRTC_SDK_APP_ID:-1400000001}
      DRONE_TRTC_SECRET_KEY: ${TRTC_SECRET_KEY:-docker-placeholder-trtc-secret-key}
      DRONE_TRTC_ROOM_EXPIRE: ${TRTC_ROOM_EXPIRE:-3600}
      DRONE_AMAP_KEY: ${AMAP_KEY:-docker-placeholder-amap-key}
      DRONE_AMAP_SECURITY_KEY: ${AMAP_SECURITY_KEY:-docker-placeholder-amap-security-key}
      JAVA_TOOL_OPTIONS: "${JAVA_TOOL_OPTIONS:--Xms256m -Xmx512m}"
    ports:
      - "${APP_PORT:-8081}:8081"
    volumes:
      - backend_logs:/app/logs

volumes:
  mysql_data:
  backend_logs:
